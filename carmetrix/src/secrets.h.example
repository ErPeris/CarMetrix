#pragma once
// ============================================================
//  CarMetrix — secrets.h (TEMPLATE)
//  Copia questo file in `secrets.h` (gitignored) e incolla i tuoi valori.
//  secrets.h NON va MAI committato.
// ============================================================

// GitHub Personal Access Token per l'OTA da repo PRIVATA.
// Lascia "" se la repo è pubblica (l'OTA funziona senza token).
// Per una repo privata: crea un token *fine-grained*, scope SOLO il repo
// CarMetrix, permesso "Contents: Read-only". Anche se estratto dal firmware
// dà accesso in sola lettura a questo solo repo.
#define GITHUB_TOKEN ""
